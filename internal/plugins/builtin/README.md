# Built-in Conven plugins

Only generic Python plugins belong in this directory. Conven embeds executable
`.py` files from this directory and installs them in the user-global scope
without overwriting an existing user copy.
